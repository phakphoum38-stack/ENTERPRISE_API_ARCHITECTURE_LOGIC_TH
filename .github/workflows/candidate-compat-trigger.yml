name: Research OS Candidate Compat Trigger

on:
  push:
    branches:
      - compat/pr41-pr47-8790
    paths:
      - '.github/workflows/**'
      - 'apps/**'
      - 'installer/**'
      - 'scripts/**'
      - 'tools/**'
      - 'research/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'

permissions:
  actions: write
  contents: read

concurrency:
  group: research-os-candidate-compat-${{ github.sha }}
  cancel-in-progress: false

jobs:
  dispatch-candidate:
    name: Dispatch exact-SHA Research OS V3 Unified 10x10 Gate
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch V3 unified candidate validation from compatibility branch
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_SHA: ${{ github.sha }}
        run: |
          # Dispatch the canonical workflow definition from main; validate the
          # exact compatibility-branch commit via target_sha inside that workflow.
          gh workflow run research-os-unified-10x10-gate.yml \
            --repo "${{ github.repository }}" \
            --ref main \
            -f target_sha="$TARGET_SHA"
