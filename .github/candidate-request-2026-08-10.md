# Candidate validation request

Purpose: validate incremental candidate evidence collection.

Policy:
- record each successful stage immediately;
- preserve partial progress when a later stage fails;
- only mark the final candidate as passed after all required validation gates pass.
