# Research OS Full Test Case Inventory\n\nM.2 has a subordinate test-case discovery control. The target band is 13,000–14,000 discovered test cases, inclusive. This is a flexible target, not a fabricated required total.\n\nPython cases are discovered from test* functions/methods through AST. Dart/Flutter cases are discovered from test() and testWidgets() invocations. Test files and test cases are counted separately. Inventory is source-SHA pinned and does not claim execution or pass. Execution completeness remains runner/CI evidence.\n