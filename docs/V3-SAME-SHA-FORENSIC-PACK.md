# Same-SHA Forensic Pack

For a target SHA, record the exact base, head, changed-file set, target-bound evidence set, and manifest identity. Reconciliation is PASS only when every evidence entry names the same target SHA and every intended changed file is accounted for. This pack is observational and does not authorize merge.
