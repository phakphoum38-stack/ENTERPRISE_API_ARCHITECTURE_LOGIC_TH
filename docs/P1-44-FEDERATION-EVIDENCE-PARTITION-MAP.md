# P1-44 federation evidence partition map
Independent projection boundary for the federation evidence layer.

Requires canonical mission/work/baseline lineage and deterministic SHA-256 metadata. No new storage, scheduler, queue, worker, execution engine, authority, merge path, or CI behavior.
