# Pairwise Scenario Planning

The pairwise planner sits above ScenarioAlgebra. It selects boundary representatives and produces a bounded set of deterministic scenarios. It is a planning/assurance primitive only and never executes runtime work. Full Cartesian cardinality remains available from ScenarioAlgebra; pairwise output is intentionally bounded.
