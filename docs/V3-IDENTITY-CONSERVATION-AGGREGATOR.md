# Identity Conservation Aggregator

Aggregates bounded scenario observations and exposes logical identity duplication without creating or executing runtime work. Missing task, event, delivery, or idempotency identity fails closed. Replay records can therefore be compared without changing the execution boundary.
