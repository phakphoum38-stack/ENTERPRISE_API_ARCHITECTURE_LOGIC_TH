name: research_os_v3_flutter
description: Research OS V3.1 Full System workspace with governed adaptive 10^10 logical capacity.
publish_to: "none"
version: 3.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
