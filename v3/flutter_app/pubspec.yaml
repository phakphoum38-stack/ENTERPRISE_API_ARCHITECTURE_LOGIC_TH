name: research_os_v3_flutter
description: Clean V3 desktop shell for Research OS.
publish_to: "none"
version: 3.0.0-dev.1+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
