include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Temporary compatibility debt: one legacy Dashboard local is unused.
    # Remove this override when the Workspace Dashboard refactor lands.
    unused_local_variable: ignore

linter:
  rules:
    avoid_print: true
