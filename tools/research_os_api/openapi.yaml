openapi: 3.1.0
info:
  title: Research OS API
  version: 2.0.0-rc.1
  description: Provider-agnostic Research OS API. V1 remains supported while the V2 draft contract is validated on the same runtime owners.
  x-research-os-v2-status: draft
servers:
  - url: http://127.0.0.1:8787
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: Service is healthy
  /v1/providers:
    get:
      summary: List supported AI provider adapters
      responses:
        "200":
          description: Provider list
  /v1/ai/generate:
    post:
      summary: Generate text through a selected provider adapter
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [prompt]
              properties:
                provider:
                  type: string
                model:
                  type: string
                system:
                  type: string
                prompt:
                  type: string
      responses:
        "200":
          description: Generated output
        "502":
          description: Provider failure
  /v1/conversations/analyze:
    post:
      summary: Analyze a conversation and return a preview Research Artifact
      description: This endpoint does not persist or publish the artifact.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [conversation]
              properties:
                title:
                  type: string
                conversation:
                  oneOf:
                    - type: string
                    - type: array
                      items:
                        type: object
                status:
                  type: string
                tags:
                  type: array
                  items:
                    type: string
                evidence:
                  type: array
                  items:
                    type: string
                relationships:
                  type: array
                  items:
                    type: string
                min_quality:
                  type: integer
      responses:
        "200":
          description: Artifact preview and quality decision
  /v1/knowledge/artifacts:
    get:
      summary: List Research Artifacts
      responses:
        "200":
          description: Artifact index
  /v1/knowledge/graph:
    get:
      summary: Return the current knowledge graph as JSON
      responses:
        "200":
          description: Graph nodes and edges
  /v1/agents:
    get:
      summary: List registered agents
      responses:
        "200":
          description: Agent catalog
  /v1/agents/readiness:
    get:
      summary: Return agent readiness status
      responses:
        "200":
          description: Agent readiness
  /v1/agents/discover:
    get:
      summary: Discover agents by capability or permission
      parameters:
        - name: capability
          in: query
          required: false
          schema:
            type: string
        - name: permission
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Matching agents
  /v1/agents/orchestrations:
    get:
      summary: List and filter Multi-Agent orchestration runs
      parameters:
        - $ref: '#/components/parameters/StatusFilter'
        - $ref: '#/components/parameters/SearchFilter'
        - $ref: '#/components/parameters/AgentFilter'
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
      responses:
        "200":
          description: Orchestration run list
        "400":
          description: Invalid filter request
    post:
      $ref: '#/components/pathItems/CreateOrchestration'
  /v1/agents/orchestrations/{run_id}:
    parameters:
      - $ref: '#/components/parameters/RunId'
    get:
      summary: Get one Multi-Agent orchestration run
      responses:
        "200":
          description: Orchestration run
        "404":
          description: Orchestration run not found
  /v1/agents/orchestrations/{run_id}/timeline:
    parameters:
      - $ref: '#/components/parameters/RunId'
    get:
      summary: Get the durable audit timeline for an orchestration run
      responses:
        "200":
          description: Ordered orchestration audit events
        "404":
          description: Orchestration run not found
  /v1/agents/orchestrations/{run_id}/execute:
    parameters:
      - $ref: '#/components/parameters/RunId'
    post:
      $ref: '#/components/pathItems/ExecuteOrchestration'
  /v1/agents/orchestrations/{run_id}/confirm:
    parameters:
      - $ref: '#/components/parameters/RunId'
    post:
      $ref: '#/components/pathItems/ConfirmOrchestration'
  /v1/agents/orchestrations/{run_id}/retry:
    parameters:
      - $ref: '#/components/parameters/RunId'
    post:
      $ref: '#/components/pathItems/RetryOrchestration'
  /v1/agents/orchestrations/{run_id}/cancel:
    parameters:
      - $ref: '#/components/parameters/RunId'
    post:
      $ref: '#/components/pathItems/CancelOrchestration'

  /v2/health/readiness:
    get:
      summary: Consolidated V2 readiness for runtime, agents, active provider, and local storage
      responses:
        "200":
          description: All required V2 components are ready
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Readiness'
        "503":
          description: One or more required V2 components are unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
  /v2/brain/skills:
    get:
      summary: List provider-neutral Brain Skills from the shared V3 registry
      responses:
        "200":
          description: Versioned Brain Skills catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2BrainCatalog'
  /v2/brain/capacity:
    get:
      summary: Describe Adaptive 6x6 capacity and bounded active-worker policy
      responses:
        "200":
          description: Theoretical 6^6 capacity and current activation limits
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2BrainCapacity'
  /v2/brain/providers:
    get:
      summary: Discover configured Brain providers without exposing credential values
      responses:
        "200":
          description: Safe provider credential and web-search capability status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2BrainProviders'
  /v2/brain/plans:
    post:
      summary: Create a provider-neutral adaptive Brain Skills dispatch plan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [objective]
              properties:
                objective:
                  type: string
                  minLength: 1
                complexity_level:
                  type: integer
                  minimum: 1
                  maximum: 6
                  default: 1
                requested_workers:
                  type: integer
                  minimum: 1
                  maximum: 46656
                budget_workers:
                  type: integer
                  minimum: 1
                ready_workers:
                  type: integer
                  minimum: 1
      responses:
        "201":
          description: Adaptive Brain Skills plan created without starting all workers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2BrainPlan'
        "400":
          description: Invalid planning request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
  /v2/brain/search:
    post:
      summary: Run bounded Compound 6x6 research with provider-hosted web search
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [query]
              properties:
                query:
                  type: string
                  minLength: 1
                  maxLength: 20000
                provider:
                  type: string
                  description: Defaults to the configured RESEARCH_OS_SEARCH_PROVIDER or openai-responses.
                model:
                  type: string
                complexity_level:
                  type: integer
                  minimum: 1
                  maximum: 6
                  default: 3
                requested_workers:
                  type: integer
                  minimum: 1
                  maximum: 46656
                budget_workers:
                  type: integer
                  minimum: 1
                ready_workers:
                  type: integer
                  minimum: 1
      responses:
        "200":
          description: Source-backed answer and bounded compound Brain plan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2BrainSearch'
        "400":
          description: Invalid search query or Brain plan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
        "503":
          description: Search provider or credential is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
  /v2/agents:
    get:
      summary: V2 agent catalog backed by the same dynamic registry as V1
      responses:
        "200":
          description: Versioned agent catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2AgentCatalog'
  /v2/agents/readiness:
    get:
      summary: V2 alias for agent readiness
      responses:
        "200":
          description: Versioned agent readiness payload
  /v2/agents/discover:
    get:
      summary: V2 alias for capability discovery
      parameters:
        - name: capability
          in: query
          required: false
          schema:
            type: string
        - name: permission
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Versioned capability discovery payload
  /v2/orchestrations:
    get:
      summary: Cursor-paginated V2 orchestration history
      parameters:
        - $ref: '#/components/parameters/StatusFilter'
        - $ref: '#/components/parameters/SearchFilter'
        - $ref: '#/components/parameters/AgentFilter'
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: cursor
          in: query
          required: false
          description: Opaque cursor returned by the previous page.
          schema:
            type: string
      responses:
        "200":
          description: Versioned orchestration page
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2OrchestrationPage'
        "400":
          description: Invalid filter, page size, or cursor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
    post:
      summary: Create an orchestration through the V2 transport contract
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [objective, steps]
              properties:
                objective:
                  type: string
                  minLength: 1
                steps:
                  type: array
                  minItems: 1
                  items:
                    $ref: '#/components/schemas/DelegatedStepInput'
      responses:
        "201":
          description: Versioned orchestration created response
        "400":
          description: Invalid orchestration request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
  /v2/orchestrations/{run_id}:
    parameters:
      - $ref: '#/components/parameters/RunId'
    get:
      summary: Get one V2 orchestration run
      responses:
        "200":
          description: Versioned orchestration response
        "404":
          description: Orchestration not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
  /v2/orchestrations/{run_id}/timeline:
    parameters:
      - $ref: '#/components/parameters/RunId'
    get:
      summary: Get the durable V2 orchestration audit timeline
      responses:
        "200":
          description: Versioned audit timeline
        "404":
          description: Orchestration not found
  /v2/orchestrations/{run_id}/execute:
    parameters:
      - $ref: '#/components/parameters/RunId'
    post:
      $ref: '#/components/pathItems/ExecuteOrchestration'
  /v2/orchestrations/{run_id}/confirm:
    parameters:
      - $ref: '#/components/parameters/RunId'
    post:
      $ref: '#/components/pathItems/ConfirmOrchestration'
  /v2/orchestrations/{run_id}/retry:
    parameters:
      - $ref: '#/components/parameters/RunId'
    post:
      $ref: '#/components/pathItems/RetryOrchestration'
  /v2/orchestrations/{run_id}/cancel:
    parameters:
      - $ref: '#/components/parameters/RunId'
    post:
      $ref: '#/components/pathItems/CancelOrchestration'
  /v2/workspaces:
    get:
      summary: List local-first V2 workspaces from the shared Workspace Knowledge Engine
      responses:
        "200":
          description: Versioned workspace catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2WorkspaceList'
        "503":
          description: Workspace index is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
  /v2/workspaces/{workspace_id}/knowledge:
    parameters:
      - $ref: '#/components/parameters/WorkspaceId'
    get:
      summary: Search cursor-paginated workspace knowledge with provenance and evidence
      parameters:
        - name: q
          in: query
          required: false
          description: Case-insensitive full-text query over title, content, tags, and evidence.
          schema:
            type: string
        - name: kinds
          in: query
          required: false
          description: Comma-separated knowledge record kinds to include.
          schema:
            type: string
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 25
        - name: cursor
          in: query
          required: false
          description: Opaque cursor returned by the previous knowledge page.
          schema:
            type: string
      responses:
        "200":
          description: Versioned knowledge search page with provenance
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2KnowledgePage'
        "400":
          description: Invalid workspace search query, page size, or cursor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
        "404":
          description: Workspace not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'
        "503":
          description: Workspace index is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2ErrorEnvelope'

components:
  parameters:
    RunId:
      name: run_id
      in: path
      required: true
      schema:
        type: string
    WorkspaceId:
      name: workspace_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    StatusFilter:
      name: status
      in: query
      required: false
      schema:
        type: string
    SearchFilter:
      name: q
      in: query
      required: false
      description: Case-insensitive search over run ID, objective, step ID, and step objective.
      schema:
        type: string
    AgentFilter:
      name: agent
      in: query
      required: false
      schema:
        type: string
  pathItems:
    CreateOrchestration:
      summary: Create a Multi-Agent orchestration run
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [objective, steps]
              properties:
                objective:
                  type: string
                  minLength: 1
                steps:
                  type: array
                  minItems: 1
                  items:
                    $ref: '#/components/schemas/DelegatedStepInput'
      responses:
        "201":
          description: Orchestration created
        "400":
          description: Invalid orchestration request
    ExecuteOrchestration:
      summary: Execute all currently runnable orchestration steps
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                confirmed:
                  type: boolean
                  default: false
      responses:
        "200":
          description: Updated orchestration run
        "400":
          description: Run cannot be executed in its current state
        "404":
          description: Orchestration run not found
    ConfirmOrchestration:
      summary: Confirm write-capable tasks waiting at the governance gate
      responses:
        "200":
          description: Confirmed and updated orchestration run
        "400":
          description: Run cannot be confirmed in its current state
        "404":
          description: Orchestration run not found
    RetryOrchestration:
      summary: Retry failed orchestration steps within their bounded attempt policy
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                step_id:
                  type: string
                  description: Retry one failed step. Omit to retry all currently retryable failed steps.
      responses:
        "200":
          description: Retried and updated orchestration run
        "400":
          description: No retryable steps or retry limit exhausted
        "404":
          description: Orchestration run not found
    CancelOrchestration:
      summary: Cancel a non-terminal orchestration run and persist the cancellation
      responses:
        "200":
          description: Cancelled orchestration run
        "400":
          description: Terminal orchestration run cannot be cancelled
        "404":
          description: Orchestration run not found
  schemas:
    V2BrainCatalog:
      type: object
      required: [api_version, brain]
      properties:
        api_version:
          const: v2
        brain:
          type: object
          required: [provider_mode, skill_count, skills]
          properties:
            provider_mode:
              const: local_or_configured_provider
            skill_count:
              type: integer
              minimum: 1
            skills:
              type: array
              items:
                type: object
                additionalProperties: true
    V2BrainCapacity:
      type: object
      required: [api_version, capacity]
      properties:
        api_version:
          const: v2
        capacity:
          type: object
          required:
            - architecture
            - branch_factor
            - elastic_tiers
            - max_leaf_capacity
            - max_active_workers
            - activation_mode
            - all_workers_started_by_default
          properties:
            architecture:
              const: adaptive_hierarchical_6x6
            branch_factor:
              const: 6
            elastic_tiers:
              const: 6
            max_leaf_capacity:
              const: 46656
            max_active_workers:
              type: integer
              minimum: 1
              maximum: 1296
            activation_mode:
              const: demand_budget_readiness
            all_workers_started_by_default:
              const: false
          additionalProperties: true
    V2BrainPlan:
      type: object
      required: [api_version, plan]
      properties:
        api_version:
          const: v2
        plan:
          type: object
          required:
            - plan_id
            - objective
            - selected_skills
            - hierarchy
            - cognition
            - provider_mode
            - requires_external_api_key
            - provider_execution_requires_credentials
            - writes_require_approval
          properties:
            plan_id:
              type: string
            objective:
              type: string
            selected_skills:
              type: array
              items:
                type: string
            hierarchy:
              type: object
              additionalProperties: true
            cognition:
              type: object
              required:
                - mode
                - stages
                - candidate_capacity
                - hypothesis_branches
                - critic_passes
                - consensus_quorum
                - bounded
              properties:
                mode:
                  const: compound_6x6
                stages:
                  type: array
                  items:
                    type: string
                candidate_capacity:
                  type: integer
                  minimum: 6
                  maximum: 46656
                hypothesis_branches:
                  type: integer
                  minimum: 1
                  maximum: 36
                critic_passes:
                  type: integer
                  minimum: 1
                  maximum: 6
                consensus_quorum:
                  type: integer
                  minimum: 1
                bounded:
                  const: true
              additionalProperties: true
            provider_mode:
              const: local_or_configured_provider
            requires_external_api_key:
              const: false
            provider_execution_requires_credentials:
              const: true
            writes_require_approval:
              const: true
    V2BrainProviders:
      type: object
      required: [api_version, providers]
      properties:
        api_version:
          const: v2
        providers:
          type: object
          additionalProperties:
            type: object
            required: [configured, secret_exposed, supports_web_search]
            properties:
              configured:
                type: boolean
              credential_source:
                type: [string, 'null']
              secret_exposed:
                const: false
              supports_web_search:
                type: boolean
    V2BrainSearch:
      type: object
      required: [api_version, result, brain_plan]
      properties:
        api_version:
          const: v2
        result:
          type: object
          required: [provider, model, text, sources]
          properties:
            provider:
              type: string
            model:
              type: string
            text:
              type: string
            sources:
              type: array
              items:
                type: object
                required: [url, title]
                properties:
                  url:
                    type: string
                    format: uri
                  title:
                    type: string
        brain_plan:
          type: object
          additionalProperties: true
    DelegatedStepInput:
      type: object
      required: [objective]
      properties:
        step_id:
          type: string
        objective:
          type: string
          minLength: 1
        requested_agent:
          type: [string, 'null']
        depends_on:
          type: array
          items:
            type: string
        context:
          type: object
          additionalProperties: true
        max_attempts:
          type: integer
          minimum: 1
          maximum: 5
          default: 3
          description: Maximum explicit execution attempts for this delegated step.
    V2OrchestrationRun:
      type: object
      required: [run_id, objective, status, steps]
      properties:
        run_id:
          type: string
        objective:
          type: string
        status:
          type: string
        created_at:
          type: number
        updated_at:
          type: number
        steps:
          type: array
          items:
            type: object
            additionalProperties: true
        events:
          type: array
          items:
            type: object
            additionalProperties: true
    V2PageMetadata:
      type: object
      required: [page_size, returned]
      properties:
        page_size:
          type: integer
          minimum: 1
          maximum: 100
        returned:
          type: integer
          minimum: 0
        next_cursor:
          type: [string, 'null']
    V2OrchestrationPage:
      type: object
      required: [api_version, items, page, filters]
      properties:
        api_version:
          const: v2
        items:
          type: array
          items:
            $ref: '#/components/schemas/V2OrchestrationRun'
        page:
          $ref: '#/components/schemas/V2PageMetadata'
        filters:
          type: object
          additionalProperties: true
    V2AgentCatalog:
      type: object
      required: [api_version, agents, count]
      properties:
        api_version:
          const: v2
        count:
          type: integer
          minimum: 0
        agents:
          type: array
          items:
            type: object
            additionalProperties: true
    V2Workspace:
      type: object
      required: [workspace_id, name, created_at, updated_at]
      properties:
        workspace_id:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        created_at:
          type: number
        updated_at:
          type: number
    V2WorkspaceList:
      type: object
      required: [api_version, workspaces, count]
      properties:
        api_version:
          const: v2
        workspaces:
          type: array
          items:
            $ref: '#/components/schemas/V2Workspace'
        count:
          type: integer
          minimum: 0
    V2Provenance:
      type: object
      required: [source_type, source_id, evidence, imported_at]
      properties:
        source_type:
          type: string
        source_id:
          type: string
        source_path:
          type: [string, 'null']
        evidence:
          type: array
          items:
            type: string
        imported_at:
          type: number
    V2KnowledgeRecord:
      type: object
      required: [record_id, workspace_id, kind, title, content, content_hash, provenance, tags, updated_at, score]
      properties:
        record_id:
          type: string
        workspace_id:
          type: string
        kind:
          type: string
        title:
          type: string
        content:
          type: string
        content_hash:
          type: string
        provenance:
          $ref: '#/components/schemas/V2Provenance'
        tags:
          type: array
          items:
            type: string
        updated_at:
          type: number
        score:
          type: integer
          minimum: 0
    V2KnowledgePage:
      type: object
      required: [api_version, workspace_id, items, page, filters]
      properties:
        api_version:
          const: v2
        workspace_id:
          type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/V2KnowledgeRecord'
        page:
          $ref: '#/components/schemas/V2PageMetadata'
        filters:
          type: object
          required: [q, kinds]
          properties:
            q:
              type: string
            kinds:
              type: array
              items:
                type: string
    V2Readiness:
      type: object
      required: [api_version, version, ready, runtime, agents, provider, storage, checked_at]
      properties:
        api_version:
          const: v2
        version:
          type: string
        ready:
          type: boolean
        runtime:
          type: object
          additionalProperties: true
        agents:
          type: object
          additionalProperties: true
        provider:
          type: object
          additionalProperties: true
        storage:
          type: object
          additionalProperties: true
        checked_at:
          type: number
    V2ErrorEnvelope:
      type: object
      required: [api_version, error]
      properties:
        api_version:
          const: v2
        error:
          type: object
          required: [code, message, status]
          properties:
            code:
              type: string
            message:
              type: string
            status:
              type: integer
