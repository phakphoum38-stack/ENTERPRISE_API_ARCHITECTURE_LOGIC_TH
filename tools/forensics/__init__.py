"""Reusable, fail-closed execution-boundary forensic primitives.

This package is diagnostic-only: it records observations and derives bounded
isolation hypotheses. It never mutates source, governance, authority, or gate
state.
"""
