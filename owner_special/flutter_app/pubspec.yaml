name: research_os_owner_special
description: Owner-only Friend Complete desktop shell for Research OS.
publish_to: "none"
version: 1.3.0-owner.3+3

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
